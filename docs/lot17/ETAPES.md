# Lot 17 — étapes

Une ligne par fiche terminée : la fiche, son verdict, l'outil standard entré
dans l'échelle s'il y en a un, ce qui reste ouvert.

Une reprise après coupure se fait en lisant ce fichier et en repartant de la
fiche suivante.

| # | Fiche | Verdict | Outil standard | Reste ouvert |
|---|---|---|---|---|
| 1 | `check-a-company-identification-number` | N0 | `python-stdnum` 2.2 / `stdnum-js` 1.12.6 — la clé, et l’exception La Poste qu’un Luhn écrit à la main rate | Le répertoire Sirene est nommé dans le verdict et les liens, il n’est pas un niveau : ce n’est pas un modèle. N1, N2 et N3 fermés. |
| 2 | `check-bank-details-before-a-transfer` | N0 | `python-stdnum` 2.2 / `ibantools` 4.5.4 pour ISO 13616 et le registre ; la clé RIB française est écrite dans l’extrait, faute de couverture identique des deux côtés | La vérification du bénéficiaire (règlement UE 2024/886) est nommée dans le verdict, elle n’est pas un niveau : c’est un service du prestataire. N1, N2 et N3 fermés. |
| 3 | `validate-a-phone-number-and-its-country-code` | N0 | `phonenumbers` 9.0.39 / `libphonenumber-js` 1.13.13 (lot « max ») | Deux divergences entre les deux bibliothèques fermées dans l’extrait (espace fine, lettres lues au clavier téléphonique). N1, N2 et N3 fermés. |
| 4 | `validate-an-email-address-without-sending-a-message` | N0 | Aucune : les deux bibliothèques (`email-validator` 2.3.0, `validator.js` 13.15.35) sont écartées avec une raison mesurée — quatre désaccords entre elles et quatorze avec le navigateur sur trente-deux adresses. L’extrait transcrit l’expression régulière du standard HTML. | Le contrôle DNS/MX est nommé dans le verdict, il n’est pas un niveau. Essai interactif, le premier du lot. N1, N2 et N3 fermés. |
| 5 | `check-a-file-is-really-the-format-it-claims` | N0 | `puremagic` 2.2.0 / `file-type` 22.1.1 | Trois divergences entre les deux bibliothèques fermées dans l’extrait (conteneurs ZIP, faux positifs de table, plancher d’octets). Les formats texte ne sont pas couverts, et c’est le point de rupture. N1, N2 et N3 fermés. |
| 6 | `know-whether-a-pdf-needs-ocr` | N0 | `pypdf` 6.1.3 / `pdf.js` 5.4.149 | Deux niveaux écrits : N0 trie page par page, N1 note les paires de lettres contre vos pages sûres et attrape la couche texte cassée. Un correctif à `scripts/check-french.py` : un « # » au milieu d’une valeur n’ouvre pas un commentaire YAML. N2 et N3 fermés. |
| 7 | `repair-text-with-broken-encoding` | N0 | `ftfy` 6.3.1 en Python ; aucun équivalent en JavaScript, où l’extrait écrit l’aller-retour windows-1252 / UTF-8 en trente lignes | Les deux extraits s’accordent sur trente-neuf des quarante et une chaînes ; les deux écarts sont la règle du « Ã » suivi d’une espace, dite dans la fiche. N1, N2 et N3 fermés. |
| 8 | `validate-an-imported-file-against-a-schema` | N0 | `jsonschema` 4.26.0 / `ajv` 8.20.0 avec `ajv-formats` 3.0.1, dialecte 2020-12 | Trois réglages non par défaut : formats assertés, toutes les erreurs, schéma compilé gardé (×55 en Python, ×6 000 en JavaScript). Point de rupture : « multipleOf: 0.01 » refuse 1 363 montants sur 10 000. N1, N2 et N3 fermés. |
| 9 | `validate-an-api-payload-against-its-contract` | N0 | `jsonschema` 4.26.0 / `ajv` 8.20.0, document OpenAPI posé à la racine du schéma et désigné par un pointeur JSON | Trois divergences fermées : précédence des chemins, `nullable` d’OpenAPI 3.0, décompte des propriétés en trop. Cache de compilation (×900 en JavaScript). Les paramètres de chemin et de requête sont hors périmètre, et la fiche le dit. N1, N2 et N3 fermés. |
| 10 | `check-a-password-against-a-policy` | N0 | Les exigences de la NIST SP 800-63B-4, et le service Have I Been Pwned en k-anonymat (préfixe de cinq caractères du SHA-1) | Niveau `stubbed` : la liste de fuites est un service, remplacé par un double local. zxcvbn est nommé et écarté avec sa raison (dictionnaires anglais dans le portage Python). N1, N2 et N3 fermés. |
| 11 | `extract-product-data-from-a-shop-page` | N0 | Les microdonnées JSON-LD de schema.org, que la boutique publie déjà pour les moteurs de recherche | Premier N3 du lot, avec sa garde : toute valeur absente du texte envoyé est écartée — et le point de rupture de N3 est que cette garde écarte aussi « EUR » quand la page écrit « € ». Un défaut de production trouvé et corrigé : l’expression régulière de repérage des blocs s’effondrait sur vingt mille balises non refermées. N1 et N2 fermés. |
| 12 | `extract-main-content-from-a-web-page` | N0 | `trafilatura` 2.2.0 / `@mozilla/readability` 0.6.0 avec `linkedom` 0.18.13 | Un désaccord mesuré entre les deux bibliothèques fermé dans l’extrait : sur une page de catégorie, `Readability` rend le texte des liens comme un article. La part du texte à l’intérieur des liens est mesurée des deux côtés. N1, N2 et N3 fermés. |
| 13 | `extract-tables-from-a-pdf` | N0 | `pdfplumber` 0.11.10 pour la lecture par les filets ; aucun équivalent en JavaScript, où l’extrait ne fait que la lecture devinée | Le repli — deviner les colonnes d’après la position des mots — est écrit dans les deux langages, parce que celui de `pdfplumber` coupe « Prix HT » en « Prix H ». Le rapport dit toujours laquelle des deux lectures a répondu. N1, N2 et N3 fermés. |
| 14 | `convert-a-pdf-to-plain-text` | N0 | `pypdf` / `pdftotext` nommés ; `pdfplumber` 0.11.10 pour les coordonnées en Python, `pdf.js` en JavaScript | La recherche de gouttières — le même code dans les deux langages — remet les colonnes dans l’ordre là où la lecture brute les entrelace. Les césures sont laissées telles quelles, et la fiche dit pourquoi. N1, N2 et N3 fermés. |
| 15 | `extract-fields-from-a-log-line` | N0 | La notation de motifs nommés de Grok, réimplémentée en une douzaine de lignes parce que Python et JavaScript n’orthographient pas le groupe nommé de la même façon | Deux règles pour lesquelles le reste du fichier existe : une ligne non reconnue revient avec son numéro, et l’horodatage est rendu tel qu’il est écrit. Point de rupture : un motif trop lâche découpe faux sans rien rejeter. N1, N2 et N3 fermés. |
| 16 | `extract-the-latest-reply-from-an-email-thread` | N0 | `email_reply_parser` 0.5.12 et `email-reply-parser` 2.3.9, nommées et mesurées, mais pas appelées | Les deux bibliothèques de référence se contredisent sur le même fil français : l’une y laisse l’en-tête « … a écrit : », l’autre retire la signature qu’elle garde en anglais. Les marqueurs sont donc écrits dans l’extrait, les français compris, et la signature n’est pas touchée. N3 ouvert pour le seul cas que N0 signale — la réponse écrite dans la citation — et il rend des numéros de ligne, pas du texte. N1 fermé sur `talon` (1.4.4, 2017), N2 sur le volume. |
| 17 | `extract-amounts-and-currencies-from-text` | N0 | `price-parser` 0.5.1, nommé et mesuré : sur une ligne de facture il rend le numéro de pièce, et son défaut de séparateur décide à mille près | La convention de séparateur est un paramètre obligatoire sans défaut, et tout nombre dont la lecture en dépend revient marqué `ambiguous`. La valeur est une chaîne de chiffres, jamais un flottant. N1 fermé sur une mesure à la source : les modèles français de spaCy portent quatre étiquettes, aucune pour une somme, là où les anglais en ont dix-huit dont MONEY. N3 ouvert pour les documents dont la devise est écrite une fois, en entête. |
| 18 | `extract-key-terms-from-a-document` | N1 | `yake` 0.7.3 (LGPLv3) et `rake-nltk` 1.0.6 en Python, `keyword-extractor` 0.3.0 en JavaScript — qui publie un paquet sans licence déclarée | Le verdict tient à un mot de l’énoncé : « caractériser », c’est distinguer, et distinguer demande une collection. Les deux niveaux partagent le découpage et la liste de mots vides ; seul le classement change. Le logarithme n’est pas planché à un comme chez scikit-learn, parce qu’un terme présent partout n’est le mot-clé d’aucun document. Point de rupture de N1 : un sujet partagé par toute la collection tombe à zéro comme le passe-partout, et c’est N0 qui le fait remonter. N2 et N3 fermés : ils lisent un document à la fois. |
| 19 | `extract-people-and-companies-from-an-article` | N2 | spaCy 3.8.16 (MIT) et ses pipelines français sous LGPL-LR ; `@huggingface/transformers` 4.3.0 côté JavaScript | Premier verdict N2 du lot, et il est assumé : la moitié difficile de l’énoncé — ce qu’un nom *est* — n’est écrite nulle part dans le texte. Le niveau N0 garde son emploi parce qu’il répond `unknown` quand la phrase ne prouve rien, là où le pipeline affirme toujours quelque chose. La langue a décidé plus que la bibliothèque : dix-huit étiquettes d’entités en anglais, quatre en français. N1 fermé : un champ aléatoire conditionnel réclame un corpus annoté. N3 fermé : coût et périmètre vont dans le même sens. |
| 20 | `extract-links-from-a-page` | N0 | `urllib.parse.urljoin` (RFC 3986) et `new URL` (norme WHATWG) — et c’est la seconde qui a raison, puisque c’est celle des navigateurs | Sept divergences mesurées sur trente-huit adresses, la plus frappante étant l’antislash : `href="\\chemin"` mène à un chemin de votre site d’un côté, à l’hôte `chemin` de l’autre. L’extrait Python a été amené à la réponse du navigateur, et les deux rendent le même rapport. La base est exigée, rendue, et remplacée par l’élément `<base href>` quand la page en porte un. N1, N2 et N3 fermés. |
| 21 | `extract-metadata-from-a-file` | N0 | La bibliothèque standard côté Python (`zipfile`, `xml.etree`) ; `fflate` 0.8.3 et `fast-xml-parser` 5.2.5 côté JavaScript | Un .docx est un ZIP dont deux entrées ne sont que des métadonnées. La règle de la fiche : les parties non lues sont **nommées**, parce qu’un document dont les auteurs sont dans ses commentaires ne doit pas répondre « aucun auteur ». Écart fermé entre les langages : `fast-xml-parser` ne décode pas « &#232; » sans son option `htmlEntities`, et le même fichier rendait deux auteurs différents. N1, N2 et N3 fermés. |
| 22 | `extract-a-table-from-a-web-page` | N0 | `html.parser` de la bibliothèque standard côté Python, `linkedom` — déjà dépendance du site — côté JavaScript ; l’algorithme est celui que la norme HTML décrit | Une cellule fusionnée est déployée sur les places qu’elle couvre et chaque répétition est marquée : c’est le seul moyen de garder les colonnes alignées sans faire croire que la page a écrit trois fois ce qu’elle a écrit une fois. Un tableau imbriqué est rendu à part et son texte ne coule pas dans la cellule qui le porte. Le plafond de portée est celui de la norme, mille. N1, N2 et N3 fermés. |
